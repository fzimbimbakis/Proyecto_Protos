#include "../include/debug.h"
#include <stdio.h>
void debug(char * etiqueta, int codigo,char * mensaje, int extra){
    if(debug)
    printf("DEBUG, %s: Código: %d. Mensaje: %s, %d", etiqueta, codigo, mensajem extra);
}

